# This file was generated by the Gradle 'init' task.
# https://docs.gradle.org/current/userguide/build_environment.html#sec:gradle_configuration_properties

org.gradle.configuration-cache=true
org.gradle.configuration-cache.problems=warn

# Parallel execution
org.gradle.parallel=true

# Build cache for faster builds
org.gradle.caching=true

# File system watching for faster incremental builds
# Disabled in container environments (Docker volumes don't support it reliably)
# Uncomment the line below if you want to enable it for local development:
# org.gradle.vfs.watch=true

# Daemon settings
org.gradle.daemon=true
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Kotlin compiler daemon
kotlin.daemon.jvmargs=-Xmx1024m -XX:MaxMetaspaceSize=256m
